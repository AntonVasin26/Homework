#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <cmath>


class Arr_Var_Vec;
class Fraction;
void as_fraction(const double& number, int& num, unsigned& denom, const int pVal);