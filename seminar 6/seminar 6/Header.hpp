#pragma once
#include <iostream>
#include <vector>
#include <algorithm>

class Arr_Var_Vec;
class Fraction;
void as_fraction(double number, int& num, unsigned& denum, int cycles = 10, double precision = 5e-4);